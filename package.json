{
  "name": "youtube-smart-playlist-creator",
  "version": "0.1.0",
  "private": true,
  "description": "Generate smart YouTube playlists from natural language prompts",
  "workspaces": [
    "client",
    "server",
    "types"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=client & npm run dev --workspace=server",
    "build": "npm run build --workspace=types && npm run build --workspace=server && npm run build --workspace=client",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present && rm -rf node_modules"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
